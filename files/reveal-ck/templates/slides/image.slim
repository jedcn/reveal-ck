- headline = headline || nil
- notes    = notes    || nil
- link     = link     || nil
- width    = width    || nil
- height   = height   || nil
- alt      = alt      || nil
ruby:
  image_helper = lambda do |src, alt, width, height|
    tag = { :tag => 'img' }
    tag[:width]  = width  if width
    tag[:height] = height if height
    tag[:alt]    = alt    if alt
    tag[:src]    = src
    tag
  end
section
  - if headline
    h2= headline
  - if link
    a.image href="#{link}" target="_blank"
      *image_helper.call(src, alt, width, height)
  - else
    *image_helper.call(src, alt, width, height)
  - if notes
    aside.notes= notes
